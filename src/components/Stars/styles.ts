import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: "flex-end",
    alignItems: "center",
  },

  canvas: {
    position: "absolute",
    height: 249,
    width: 257,
    zIndex: 1
  }
});